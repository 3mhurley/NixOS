# Replace with output of: age-keygen -y /var/lib/sops-nix/key.txt
# Optionally add your personal age key too, so you can edit secrets off-machine.
keys:
  - &citadel age1REPLACEME_HOST_PUBLIC_KEY
creation_rules:
  - path_regex: secrets/.*\.yaml$
    key_groups:
      - age:
          - *citadel
